# OpenAI API key (required for LLM analysis). Do not commit a real key.
OPENAI_API_KEY=

# Optional: model name (default: gpt-4)
OPENAI_MODEL=gpt-4

# Optional: max retries for LLM validation failures (default: 3)
MAX_RETRIES=3

# Optional: log level (default: INFO)
LOG_LEVEL=INFO
